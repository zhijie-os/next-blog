/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: process.env.SITE_URL || 'https://zhijiexia.dev' || 'https://www.zhijiexia.dev',
    // generateRobotsTxt: true, // (optional)
    // ...other options
}